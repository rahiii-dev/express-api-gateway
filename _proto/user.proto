syntax = "proto3";

package userPackage;

service userService {
    rpc GetUsers (Empty) returns (GetUsersResponse);
}

message User {
    string id = 1;
    string fullName = 2;
    string email = 3;
    string password = 4;
}

message GetUsersResponse {
    repeated User users = 1;
}

message Empty {}
